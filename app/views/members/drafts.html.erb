<h2>草稿</h2>
<ul>
  <% @drafts.each do |post| %>
    <li>   
      <%= link_to post.title, post_path(post) %> |
      <%= link_to 'Edit', posts_path(id: post.id) %>
      <%= post.content %>
    </li>
  <% end %>
</ul>